# Setting up the Demo Code

1. Install Node and TypeScript on your local computer.
2. Run `npm install` to install dependencies.
3. Run `tsc` to compile TypeScript into JavaScript in the `dist` folder.
4. Run `node dist/index.js` to see the prompt to enter a new Todo item, or add the `--show` flag to show the current Todo items.